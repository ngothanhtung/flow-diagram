/** Time spent revealing an active connector from its source to destination. */
export const EDGE_DRAW_DURATION_MS = 760;

/** Destination nodes power on immediately after their incoming line is drawn. */
export const NODE_FADE_DURATION_MS = 200;
